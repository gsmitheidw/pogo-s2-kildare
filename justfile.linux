
build msg="Update index":
    python s2.py
    git add index.html
    git commit -m "{{msg}}"
    git push

clean:
    rm -f index.html

rebuild: clean build

csv:
    git add kildare_poi.csv
    git commit -m 'update csv'
    git push


validate:
    python validate_csv.py 
